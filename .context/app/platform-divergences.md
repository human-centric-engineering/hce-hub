# Platform-divergence ledger

Every place HCE Hub has **edited a Sunrise-owned (platform) file** — the "keep-mine"
edits that can conflict when we `git merge vX.Y.Z` from upstream — plus any
**upstream asks** (fork-first seams awaiting an upstream issue/landing).

This is the fork's honest record of where it has stepped outside the designed seams.
It lives under `.context/app/` (fork-owned; Sunrise never writes here), so the ledger
itself never conflicts. Keep it current: **when you edit a platform-owned file, add a
row here in the same PR** — otherwise the edit is rediscovered (and maybe clobbered) at
the next upstream merge.

> **Not everything belongs here.** Editing a **fork-owned `lib/app/*` scaffold** (e.g.
> filling `lib/app/eslint.config.mjs`, `lib/app/public-nav.ts`, `lib/app/capabilities.ts`)
> is _not_ a divergence — those ship empty precisely to be filled and merge cleanly. Only
> log edits to files Sunrise **owns and keeps evolving** (core `lib/`, `app/api/v1`, core
> `components/`, `proxy.ts`, root config, platform tests, migration SQL, `CLAUDE.md` below
> the banner, etc.). See [`README.md`](./README.md) for the full ownership boundary.

## How to read a row

- **File** — the platform-owned path we edited.
- **Change** — what we did, minimally.
- **Why** — the forcing reason (usually: a fork-owned seam we filled has a downstream
  effect a platform file asserts/renders).
- **Merge action** — what to do when this file conflicts on an upstream sync.
- **Upstream** — `none` (fork-owned forever, no upstream change wanted) · `worth-noting`
  (a low-priority DX note to file once we can point to the pattern) · `issue #N` (filed) ·
  `landed vX.Y.Z` (delete this row).

---

## Carried edits to platform-owned files

| #   | File                                                   | Change                                                                                                                                                                                                                                                                              | Why                                                                                                                                                                                                                                                                                                                                                           | Merge action                                                                                                                                                                    | Upstream                                                                                                                                                                                                                                                                                    |
| --- | ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `tests/unit/lib/app/defaults.test.ts`                  | Two seam-default assertions now track the fork's intentional fills: `appEslintConfig` expects `[{ ignores: ['.context/app/planning/**'] }]` (not `[]`), and the public-nav case expects `publicNavItems`/`footerNavItems` `[]` with `footerLegalItems` still `null` (not all-null). | We filled the fork-owned `lib/app/eslint.config.mjs` and `lib/app/public-nav.ts` seams. The Sunrise test asserts every `lib/app/*` seam ships **empty** — a vanilla-Sunrise self-guard a fork necessarily diverges from the moment it fills a seam. Each edit preserves the guard: a _stray_ eslint block still fails, and the legal cluster is still pinned. | Keep-mine on these assertions; take any upstream additions to the _other_ assertions. This file diverges further as v1 fills more seams (capabilities, admin-nav, emails).      | `worth-noting` — the pattern is now concrete (two assertions here already). Once f-hub-capabilities fills a third, file a low-priority DX note: forks should be expected to update these assertions, and/or split the pure-invariant assertions from the content/effect ones. Non-blocking. |
| 2   | `.prettierignore`                                      | Appended a fork-owned block ignoring `.context/app/planning/`.                                                                                                                                                                                                                      | The design handoff bundles prototype `.jsx`/`.css`/`.html` and the planning markdown uses column-aligned Obsidian tables/wikilinks Prettier collapses — authoring material with no build impact.                                                                                                                                                              | Keep-mine (an append at end). The file already establishes this pattern (remediation-ledger, `settings.local.json`), so upstream edits are additive too — conflict risk is low. | `none` — fork-specific path.                                                                                                                                                                                                                                                                |
| 3   | `app/(public)/page.tsx`                                | Reduced Sunrise's marketing landing to `redirect('/dashboard')` (marked `app:shim`).                                                                                                                                                                                                | HCE Hub is auth-only — no public landing (§13.1). `/` bounces into the app; the proxy sends signed-out visitors on to `/login`; `f-shell` later reclaims `/` as the real Hub home.                                                                                                                                                                            | Keep-mine (one-line redirect). If Sunrise reworks the landing, resolve "keep mine (redirect)".                                                                                  | `none` — the documented CUSTOMIZATION §6 auth-only pattern.                                                                                                                                                                                                                                 |
| 4   | `app/(public)/about/page.tsx`                          | Deleted (marketing About page).                                                                                                                                                                                                                                                     | Auth-only app; no marketing surface (§13.1).                                                                                                                                                                                                                                                                                                                  | Delete/modify conflict → "keep mine (deleted)" if Sunrise later edits it.                                                                                                       | `none` — documented CUSTOMIZATION §6 page removal.                                                                                                                                                                                                                                          |
| 5   | `app/sitemap.ts`                                       | Dropped the root `''` (now a redirect) and `/about` (deleted) entries; kept `/contact` `/privacy` `/terms`.                                                                                                                                                                         | A sitemap must not advertise a redirecting root or a 404'd page; an auth-only app has no public marketing to index.                                                                                                                                                                                                                                           | Keep-mine on the `publicPages` list; take upstream structural changes to the generator.                                                                                         | `none`.                                                                                                                                                                                                                                                                                     |
| 6   | `tests/unit/components/layouts/public-nav.test.tsx`    | Adapted the "renders the platform default links when no override is set" case to assert the fork's curated **empty** header nav.                                                                                                                                                    | Curating the fork-owned seam (`lib/app/public-nav.ts` → `publicNavItems: []`) falsifies the test's "no override" premise — the same seam-vs-default-test coupling as row 1.                                                                                                                                                                                   | Keep-mine on that one case; the override-replacement / active-state cases are unaffected.                                                                                       | `worth-noting` — same DX note as row 1 (fork-fills-seam vs default-asserting platform test).                                                                                                                                                                                                |
| 7   | `tests/unit/components/layouts/public-footer.test.tsx` | Adapted the default-nav case: assert the marketing footer nav is gone while legal links + copyright + Cookie Preferences remain.                                                                                                                                                    | `footerNavItems: []` in the fork seam removes the Home/About/Contact cluster; the legal cluster is left at the platform default.                                                                                                                                                                                                                              | Keep-mine on that one case; the wholesale-replacement and always-on-consent cases are unaffected.                                                                               | `worth-noting` — same DX note as row 1.                                                                                                                                                                                                                                                     |
| 8   | `tests/unit/lib/db/drift-probes.test.ts`               | Adapted the "registers zero probes by default" case to assert the fork's satellite-FK → `user` probe set, in registration order (six as of f-data-model t-3; grows as later `app_*` tables add hand-FKs).                                                                           | Filling the fork-owned `lib/app/db-drift.ts` (f-data-model) falsifies the "scaffold ships empty" premise — the same seam-vs-default-test coupling as row 1.                                                                                                                                                                                                   | Keep-mine on that one case; the merge/registry-behaviour cases are unaffected.                                                                                                  | `worth-noting` — third instance of the pattern (row 1 holds two); reinforces the DX note that forks are expected to update these default-empty assertions.                                                                                                                                  |

## Upstream asks

Requests filed against Sunrise from fork experience — **fork-first seams** (a generic seam
carried _into_ a core file, awaiting an upstream issue + landing — the `[[upstream-asks]]`
concept from the feature-plan guide §8) **and** other clarifications the fork surfaced (docs,
structure).

| Issue                                                                          | Kind             | Raised by      | Ask                                                                                                                                                                                                                     | Status                                                                                                                                                                  |
| ------------------------------------------------------------------------------ | ---------------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [sunrise#429](https://github.com/human-centric-engineering/sunrise/issues/429) | docs / structure | `f-data-model` | Fork docs imply `prisma/schema/app.prisma` is fork-reserved, but it ships platform models. Clarify it's shared **or** move Sunrise's app-tier models to a platform-owned file (migration-free) so it's genuinely empty. | **open** — HCE Hub works around it (models coexist, `app_*`-prefixed); our docs corrected to say "shared". Adopt on landing if resolution (B) makes `app.prisma` empty. |

**No fork-first seams yet.** The v1 plan's tier analysis found **zero core→fork seams / zero
upstream gating** — the Hub builds entirely through existing fork-owned `lib/app/*` seams. If
a feature surfaces a genuine core-seam gap at build (the two watch-items — theme sync-seam,
per-project agent provisioning — _might_), add a row here and make filing the issue that
feature's own Done-when.

---

## Changelog

- **2026-07-11** — f-fork t-1 (auth-only shell): rows 3–7 (the marketing strip — landing
  redirect, About deletion, sitemap trim, and the two nav/footer default-test adaptations
  from curating `lib/app/public-nav.ts`).
- **2026-07-11** — Ledger created (PR #5, the planning + f-fork-claim docs PR). Seeded with
  rows 1–2: the `defaults.test.ts` eslint assertion and the `.prettierignore` append.
