# Platform-divergence ledger

Every place HCE Hub has **edited a Sunrise-owned (platform) file** — the "keep-mine"
edits that can conflict when we `git merge vX.Y.Z` from upstream — plus any
**upstream asks** (fork-first seams awaiting an upstream issue/landing).

This is the fork's honest record of where it has stepped outside the designed seams.
It lives under `.context/app/` (fork-owned; Sunrise never writes here), so the ledger
itself never conflicts. Keep it current: **when you edit a platform-owned file, add a
row here in the same PR** — otherwise the edit is rediscovered (and maybe clobbered) at
the next upstream merge.

> **Not everything belongs here.** Editing a **fork-owned `lib/app/*` scaffold** (e.g.
> filling `lib/app/eslint.config.mjs`, `lib/app/public-nav.ts`, `lib/app/capabilities.ts`)
> is _not_ a divergence — those ship empty precisely to be filled and merge cleanly. Only
> log edits to files Sunrise **owns and keeps evolving** (core `lib/`, `app/api/v1`, core
> `components/`, `proxy.ts`, root config, platform tests, migration SQL, `CLAUDE.md` below
> the banner, etc.). See [`README.md`](./README.md) for the full ownership boundary.

## How to read a row

- **File** — the platform-owned path we edited.
- **Change** — what we did, minimally.
- **Why** — the forcing reason (usually: a fork-owned seam we filled has a downstream
  effect a platform file asserts/renders).
- **Merge action** — what to do when this file conflicts on an upstream sync.
- **Upstream** — `none` (fork-owned forever, no upstream change wanted) · `worth-noting`
  (a low-priority DX note to file once we can point to the pattern) · `issue #N` (filed) ·
  `landed vX.Y.Z` (delete this row).

---

## Carried edits to platform-owned files

| #   | File                                                         | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Merge action                                                                                                                                                                    | Upstream                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `tests/unit/lib/app/defaults.test.ts`                        | **Restructured upstream in v0.8.0** into a data-driven `SEAM_DEFAULTS` table (one row per seam) plus a directory drift-guard. The fork now **pins** five rows to its filled values — `eslint.config.mjs`, `public-nav.ts`, `admin-nav.ts`, `protected-routes.ts` and the new `auth-landing.ts` — and adds two entries to `UNASSERTED_SEAMS`: `data-export.ts` (filled; pinning it would put a live database behind a unit test, so the fork's own `tests/unit/lib/app/data-export.test.ts` covers it) and `hub-modules.ts` (not a Sunrise seam at all — a fork-authored registry that happens to live in `lib/app/`). | We filled the fork-owned `lib/app/eslint.config.mjs`, `lib/app/public-nav.ts`, and `lib/app/admin-nav.ts` seams. The Sunrise test asserts every `lib/app/*` seam ships **empty** — a vanilla-Sunrise self-guard a fork necessarily diverges from the moment it fills a seam. Each edit preserves the guard: a _stray_ eslint block still fails, the legal cluster is still pinned, and a stray second nav section (or a lost Projects item) still fails. | Keep-mine on these assertions; take any upstream additions to the _other_ assertions. This file diverges further as v1 fills more seams (capabilities, emails).                 | **Largely addressed in v0.8.0** — upstream restructured this file around the fork case and documents the convention in its header ("filling a seam is EXPECTED to fail a row here… **pin the new value** rather than deleting the row"), which is what the fork now does. The residual DX gap moved to row 18: _other_ platform tests still hardcode a seam's default. |
| 2   | `.prettierignore`                                            | Appended a fork-owned block ignoring `.context/app/planning/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | The design handoff bundles prototype `.jsx`/`.css`/`.html` and the planning markdown uses column-aligned Obsidian tables/wikilinks Prettier collapses — authoring material with no build impact.                                                                                                                                                                                                                                                         | Keep-mine (an append at end). The file already establishes this pattern (remediation-ledger, `settings.local.json`), so upstream edits are additive too — conflict risk is low. | `none` — fork-specific path.                                                                                                                                                                                                                                                                                                                                           |
| 3   | `app/(public)/page.tsx`                                      | **Deleted** (was f-fork's `redirect('/dashboard')` shim). `f-shell` reclaims `/` as the real Hub home in the `(hub)` group.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | HCE Hub is auth-only — no public landing (§13.1). `f-shell` reclaims `/` as the protected Hub home (the `(hub)` group self-guards); a signed-out visitor is bounced to `/login`.                                                                                                                                                                                                                                                                         | Delete/modify conflict → "keep mine (deleted)" if Sunrise edits the landing (`/` is the Hub home now).                                                                          | `none` — the documented CUSTOMIZATION §6 auth-only pattern.                                                                                                                                                                                                                                                                                                            |
| 4   | `app/(public)/about/page.tsx`                                | Deleted (marketing About page).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Auth-only app; no marketing surface (§13.1).                                                                                                                                                                                                                                                                                                                                                                                                             | Delete/modify conflict → "keep mine (deleted)" if Sunrise later edits it.                                                                                                       | `none` — documented CUSTOMIZATION §6 page removal.                                                                                                                                                                                                                                                                                                                     |
| 5   | `app/sitemap.ts`                                             | Dropped the root `''` (now a redirect) and `/about` (deleted) entries; kept `/contact` `/privacy` `/terms`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | A sitemap must not advertise a redirecting root or a 404'd page; an auth-only app has no public marketing to index.                                                                                                                                                                                                                                                                                                                                      | Keep-mine on the `publicPages` list; take upstream structural changes to the generator.                                                                                         | `none`.                                                                                                                                                                                                                                                                                                                                                                |
| 6   | `tests/unit/components/layouts/public-nav.test.tsx`          | Adapted the "renders the platform default links when no override is set" case to assert the fork's curated **empty** header nav.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Curating the fork-owned seam (`lib/app/public-nav.ts` → `publicNavItems: []`) falsifies the test's "no override" premise — the same seam-vs-default-test coupling as row 1.                                                                                                                                                                                                                                                                              | Keep-mine on that one case; the override-replacement / active-state cases are unaffected.                                                                                       | `worth-noting` — same DX note as row 1 (fork-fills-seam vs default-asserting platform test).                                                                                                                                                                                                                                                                           |
| 7   | `tests/unit/components/layouts/public-footer.test.tsx`       | Adapted the default-nav case: assert the marketing footer nav is gone while legal links + copyright + Cookie Preferences remain.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `footerNavItems: []` in the fork seam removes the Home/About/Contact cluster; the legal cluster is left at the platform default.                                                                                                                                                                                                                                                                                                                         | Keep-mine on that one case; the wholesale-replacement and always-on-consent cases are unaffected.                                                                               | `worth-noting` — same DX note as row 1.                                                                                                                                                                                                                                                                                                                                |
| 8   | `tests/unit/lib/db/drift-probes.test.ts`                     | Adapted the "registers zero probes by default" case to assert the fork's satellite-FK → `user` probe set, in registration order (six as of f-data-model t-3; grows as later `app_*` tables add hand-FKs).                                                                                                                                                                                                                                                                                                                                                                                                             | Filling the fork-owned `lib/app/db-drift.ts` (f-data-model) falsifies the "scaffold ships empty" premise — the same seam-vs-default-test coupling as row 1.                                                                                                                                                                                                                                                                                              | Keep-mine on that one case; the merge/registry-behaviour cases are unaffected.                                                                                                  | `worth-noting` — third instance of the pattern (row 1 holds two); reinforces the DX note that forks are expected to update these default-empty assertions.                                                                                                                                                                                                             |
| 9   | `tests/unit/lib/orchestration/capabilities/registry.test.ts` | Made the "registerBuiltInCapabilities is idempotent" case **count-agnostic** — assert the second call adds zero registrations (capture the first-call count, assert unchanged, `≥ 13`) instead of the hard-coded `toHaveBeenCalledTimes(13)`.                                                                                                                                                                                                                                                                                                                                                                         | Filling the fork-owned `lib/app/capabilities.ts` seam (f-hub-capabilities: `next_task`, …) makes `registerBuiltInCapabilities()` flush 13 built-ins **plus** the fork's app caps, so the first-call total exceeds 13 — the same seam-vs-default-test coupling as row 1, now on the capability registry. The idempotency invariant (2nd call = no-op) is preserved and the count no longer re-breaks as t-2/t-3 add caps.                                 | Keep-mine on that one case; the built-in-presence and app-registration cases are unaffected.                                                                                    | `worth-noting` — fourth instance of the pattern; strengthens the row-1 DX note (a Sunrise-owned default/count test the fork must adapt on filling a seam).                                                                                                                                                                                                             |
| 10  | `tests/unit/components/brand/brand-mark.test.tsx`            | Rewrote the default-behaviour assertions (bare `BRAND.name` string, no wrapper element) to the fork's mark: an `aria-label`'d wrapper carrying the brand name + a decorative `aria-hidden` "H".                                                                                                                                                                                                                                                                                                                                                                                                                       | Filling the fork-owned `components/brand/brand-mark.tsx` scaffold (f-theme) falsifies the Sunrise test's "renders a bare string / no wrapper" premise — the same seam-vs-default-test coupling as row 1, on the brand-mark render. The accessibility contract (`BRAND.name` is the accessible name, still env-driven) is preserved and strengthened.                                                                                                     | Keep-mine on this file; take upstream additions structurally.                                                                                                                   | `worth-noting` — fifth instance of the pattern; reinforces the row-1 DX note (a Sunrise-owned default test the fork must adapt on filling a scaffold).                                                                                                                                                                                                                 |

| 17 | `app/(public)/contact/page.tsx` | Body copy: `Have a question about {BRAND.name}?` (was the hardcoded `Sunrise`). | The **residue of row 11**. sunrise#432 landed in v0.8.0 and fixed the `*_TAB_TITLES` constants and the three legal pages' metadata — but missed this one body-copy line, so the merged file still read "Have a question about Sunrise?" on a fork. Everything else in row 11 was taken from upstream. | Keep-mine on that line; take upstream changes to the rest of the page. | `worth-noting` — a one-line follow-up to the closed [#432](https://github.com/human-centric-engineering/sunrise/issues/432); worth a drive-by PR upstream rather than an issue. |
| 18 | `tests/unit/lib/security/proxy.test.ts`, `tests/unit/app/(auth)/verify-email/callback/page.test.tsx`, `tests/unit/app/admin/layout.test.tsx`, `tests/unit/components/forms/{accept-invite-form,oauth-button,oauth-buttons,signup-form}.test.tsx` | Landing assertions now read the seam — `AUTH_LANDING_ROUTE` / `AUTH_LANDING_LABEL` — instead of the literal `/dashboard` / `"Dashboard"`. The proxy cases were also **strengthened** from `toContain('/dashboard')` to an exact `new URL(location).pathname` match (a `toContain('/')` would have been trivially true). | Filling the **new** `lib/app/auth-landing.ts` seam (v0.8.0, #473) is the supported way to move the landing route — but seven platform test files still hardcode the platform default, so filling the seam correctly breaks 11 assertions. The sixth instance of the row-1 pattern, and the sharpest: the seam is brand-new and its own `defaults.test.ts` explicitly invites forks to fill it. | Keep-mine. Written to read the seam rather than to say `/`, so they are **fork-agnostic** — upstream could take these edits verbatim and the divergence would disappear. | [sunrise#530](https://github.com/human-centric-engineering/sunrise/issues/530) — **filed**. A platform test asserting a seam's default should read the seam; the fix is exactly the diff carried here, written fork-agnostically so upstream can take it near-verbatim. Delete this row on landing. |
| 19 | `tests/unit/lib/privacy/export-sources.test.ts` | Added `ProjectMember`, `TaskClaim`, `ProjectEvent` to `HANDLED_OUTSIDE_MANIFEST`, each pointing at the fork collector that exports it. | The v0.8.0 subject-access guard (#467) scans **every** `prisma/schema/*.prisma` — including the fork-reserved `app.prisma` — but the manifest it checks is core's and cannot hold fork rows. So a fork with app tables fails the guard even when it has correctly implemented `collectAppSubjectData`, the seam Sunrise provides for exactly this. Note also that the guard's heuristic only flags models with **no** `@relation`, so it misses our `Task.claimedByUserId` etc. — the fork's own guard (`tests/unit/lib/app/data-export.test.ts`) is what actually covers us. | Keep-mine; extend the list when a new `app_*` model holds a user column. | [sunrise#530](https://github.com/human-centric-engineering/sunrise/issues/530) — **filed** (instance 2). The core guard should consult a fork hook, or skip `app.prisma` and let the fork's own guard own it, so `HANDLED_OUTSIDE_MANIFEST` — explicitly "an accounting note, not an escape hatch" — doesn't become the fork dumping ground. The issue also carries the secondary `@relation`-heuristic bug. Delete this row on landing. |
| 20 | `scripts/smoke/export.ts` | Replaced the `check(Object.keys(bundle.app).length === 0, 'app seam is empty in vanilla Sunrise')` assertion with two: the app section carries **exactly** the Hub's seven declared subject-data sections, and every one is **empty for a synthetic subject with no Hub rows**. | Same class as rows 18–19, third surface: a **vanilla-Sunrise premise the fork falsifies by correctly filling a seam** — here `lib/app/data-export.ts`. Caught by **CI, not local gates** (`smoke:export` is a CI job, not part of `validate` or `npm test`). The replacement is strictly stronger in fork context: the old check only proved the seam was untouched, while "exactly these sections, all empty" also catches a renamed/stray section **and** a collector that matched a stranger's rows — the leak this smoke test exists to detect. | Keep-mine; extend the section list when `collectAppSubjectData` gains one. | [sunrise#530](https://github.com/human-centric-engineering/sunrise/issues/530) — **filed** (instance 3). Same fix shape as row 18, different file; the issue also flags that this one is reachable only from CI, never the local gates. |
| 16 | `app/layout.tsx` | Root `metadata` title now driven fully by the `BRAND` seam — `{ default: BRAND.name, template: '%s - <brand>' }` — dropping the hardcoded `- Next.js Starter` suffix; the starter-template `description` de-startered to `${BRAND.name} — internal operations platform.` | **Brand leak, same class as the retired row 11.** The vanilla title hardcodes `- Next.js Starter` (and a starter-template description), so a fork's every **un-templated** tab (e.g. the account `/dashboard`) reads `<brand> - Next.js Starter`. The `BRAND` seam already exists to drive `<title>`; the suffix bypasses its intent. The template also brands the account/admin/auth pages that only set a plain title. | Keep-mine on the metadata block; take upstream structural changes to the root layout. | [sunrise#519](https://github.com/human-centric-engineering/sunrise/issues/519) — filed. Same brand-leak class as the **closed** [#432](https://github.com/human-centric-engineering/sunrise/issues/432) (which fixed the `*_TAB_TITLES` + legal pages but missed the root layout). Delete this row on landing. |

## Upstream asks

Requests filed against Sunrise from fork experience — **fork-first seams** (a generic seam
carried _into_ a core file, awaiting an upstream issue + landing — the `[[upstream-asks]]`
concept from the feature-plan guide §8) **and** other clarifications the fork surfaced (docs,
structure).

| Issue                                                                          | Kind             | Raised by                    | Ask                                                                                                                                                                                                                                                                                                     | Status                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------ | ---------------- | ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [sunrise#429](https://github.com/human-centric-engineering/sunrise/issues/429) | docs / structure | `f-data-model`               | Fork docs imply `prisma/schema/app.prisma` is fork-reserved, but it ships platform models. Clarify it's shared **or** move Sunrise's app-tier models to a platform-owned file (migration-free) so it's genuinely empty.                                                                                 | **LANDED v0.8.0** — resolution (B). `app.prisma` now ships empty; `ContactSubmission`/`FeatureFlag`/`AuthBootstrap` moved to `platform.prisma` with no migration. Adopted on merge: dropped them from our copy, took upstream's header. The `app_*` prefix is kept deliberately — it still distinguishes Hub tables in the database, and renaming would be a migration with no upside. |
| [sunrise#432](https://github.com/human-centric-engineering/sunrise/issues/432) | bug              | f-theme (validation)         | `SETTINGS_TAB_TITLES` / `KNOWLEDGE_TAB_TITLES` hardcode `Sunrise` and are written to `document.title` by `useUrlTabs`, overriding the `BRAND`-templated metadata title. Also the `(public)` legal-page metadata. Use `BRAND.name`.                                                                      | **LANDED v0.8.0** — row 11 deleted; both tab-title constants and the three legal pages taken from upstream. **One line was missed upstream** (the contact-page body copy), now carried as row 17.                                                                                                                                                                                      |
| [sunrise#433](https://github.com/human-centric-engineering/sunrise/issues/433) | bug              | f-shell (t-1 review)         | Route-group error boundaries (`(protected)`/`(public)`/`admin` `error.tsx`) double-log + double-report to Sentry on session-expiry — `isSessionExpired` is both set inside the logging `useEffect`'s `checkSession()` and in its deps, so the effect self-triggers. Log/report once (deps `[error]`).   | **LANDED v0.8.0** (via #434's shared boundary). Nothing to delete — the fork never keep-mine'd the platform boundaries. **Follow-up:** `app/(hub)/error.tsx` can now become a thin `<RouteErrorBoundary>` wrapper.                                                                                                                                                                     |
| [sunrise#434](https://github.com/human-centric-engineering/sunrise/issues/434) | proposal / seam  | f-shell (t-2)                | Extract a shared `<RouteErrorBoundary>` so each route group's `error.tsx` is a thin wrapper (they're ~90-line near-duplicates). Surfaced as **the first fork to add a route group** (`(hub)`) → a 4th near-duplicate. Fixes #433 in one place + gives forks a clean primitive.                          | **LANDED v0.8.0** — `components/errors/route-error-boundary.tsx` ships upstream. **Follow-up:** adopt it in `app/(hub)/error.tsx`, currently a near-duplicate of the old shape.                                                                                                                                                                                                        |
| [sunrise#435](https://github.com/human-centric-engineering/sunrise/issues/435) | proposal / seam  | f-project-admin (t-1 review) | Add `validatePathParam(raw, schema, field)` to core `lib/api/validation.ts` — ~18 `[id]` routes each hand-roll a local `parse<X>Id` (CUID `safeParse` → `ValidationError`); no path-param validator sits alongside `validateRequestBody`/`validateQueryParams`. First fork admin CRUD reproduced it 3×. | **LANDED v0.8.0** — `validatePathParam()` is in core `lib/api/validation.ts`. **Follow-up:** retire the fork-owned `lib/api/route-params.ts` (`parseCuidParam`) and repoint its 3 callers, per the delete-on-landing action.                                                                                                                                                           |

| [sunrise#530](https://github.com/human-centric-engineering/sunrise/issues/530) | proposal / DX | v0.8.0 merge | **Platform tests should read a seam's value, not hardcode its default.** Filling the new `lib/app/auth-landing.ts` (#473) — the supported way to move the landing route — breaks 11 assertions across 7 platform test files that hardcode `/dashboard` / `"Dashboard"`. Its own `defaults.test.ts` invites forks to fill the seam, so the break is guaranteed for anyone who does. Fix: import `AUTH_LANDING_ROUTE` / `AUTH_LANDING_LABEL` in those assertions. | **filed 2026-08-04** — covers rows 18, 19 **and** 20 as one shape. Row 18 carries the diff, written fork-agnostically so upstream could take it verbatim. Delete rows 18–20 on landing. |
| [sunrise#530](https://github.com/human-centric-engineering/sunrise/issues/530) | proposal / seam | v0.8.0 merge | **The subject-access guard needs a fork hook.** `export-sources.test.ts` scans every `prisma/schema/*.prisma`, including the fork-reserved `app.prisma`, but checks a core-only manifest — so a fork fails the guard even after correctly implementing `collectAppSubjectData`, the seam provided for exactly this. Its only escape is `HANDLED_OUTSIDE_MANIFEST`, explicitly "an accounting note, not an escape hatch". Fix: consult a fork hook, or skip `app.prisma` and let the fork's own guard own it. Secondary: the "no `@relation`" heuristic misses fork models that carry intra-fork relations alongside a user-id scalar. | **filed 2026-08-04** — same issue as row 18 (instance 2 of three). Row 19 carries the workaround. Delete on landing. |

**Five asks landed in v0.8.0** ([#429](https://github.com/human-centric-engineering/sunrise/issues/429),
[#432](https://github.com/human-centric-engineering/sunrise/issues/432),
[#433](https://github.com/human-centric-engineering/sunrise/issues/433),
[#434](https://github.com/human-centric-engineering/sunrise/issues/434),
[#435](https://github.com/human-centric-engineering/sunrise/issues/435)) — every ask the fork
had filed before this merge. Five keep-mine rows (11–15) retired as a result, and rows 12–13
dissolved into a **seam fill** rather than an edit, because v0.8.0 shipped the landing seam
(#473) the fork had been hand-patching. Row numbers are not reused: gaps are the history.

**One fork-first helper so far ([sunrise#435](https://github.com/human-centric-engineering/sunrise/issues/435)).**
A fork-owned convenience (`lib/api/route-params.ts` → `parseCuidParam`) with a delete-on-landing
action — a DX/DRY improvement the fork surfaced, **not** a _gating_ core→fork seam. The v1 plan's
tier analysis still holds: **zero core→fork seams / zero upstream gating** — the Hub builds
entirely through existing fork-owned `lib/app/*` seams. If a feature surfaces a genuine core-seam
gap at build (the two watch-items — theme sync-seam, per-project agent provisioning — _might_),
add a row here and make filing the issue that feature's own Done-when.

---

## Changelog

- **2026-08-04** — **Sunrise v0.8.0 merge.** The biggest net reduction in the ledger so far: all
  five filed upstream asks landed, retiring rows **11–15**. Row 11 (`*_TAB_TITLES` + legal pages)
  went with [#432](https://github.com/human-centric-engineering/sunrise/issues/432) — leaving only
  a body-copy line upstream missed, now **row 17**. Rows 14–15 (MCP registry warming) went with
  [#457](https://github.com/human-centric-engineering/sunrise/issues/457); upstream's test mocks
  the registry module directly, which is cleaner than the fork's partial-mock workaround, so its
  test was taken wholesale. Rows **12–13** (post-login landing) did not need re-carrying at all:
  v0.8.0 shipped the landing seam (#473), so the hand-edits to `proxy.ts` and `login-form.tsx`
  became a one-line fill of `lib/app/auth-landing.ts` (`/` + label `Hub`). That fill also
  **widens** the Hub-first landing to the doors previously left on `/dashboard` (signup, OAuth,
  invite, verify) — an owner decision, taken because the split's rationale (showing new users the
  verification card) is unreachable in production: with `requireEmailVerification` an unverified
  user never holds a session, so email signup goes to `/verify-email` and invite acceptance sets
  `emailVerified` before signing in. Two new rows in the other direction: **18** (7 platform test
  files whose landing assertions hardcoded `/dashboard`) and **19** (the subject-access guard
  can't see a fork's collector), plus **20** (`smoke:export` asserts the app seam is empty — caught
  by CI, not the local gates) — all three filed together as
  [sunrise#530](https://github.com/human-centric-engineering/sunrise/issues/530), since they are one
  shape: a platform assertion about a seam's default, which any fork falsifies by using the seam
  correctly. Row 1 rewritten:
  upstream restructured `defaults.test.ts` around the fork case and now documents the
  pin-don't-delete convention, which is the DX note this ledger had been accumulating evidence for.

- **2026-07-15** — f-project-admin t-2 (admin UI): row 1 updated — filling the fork-owned `lib/app/admin-nav.ts` seam
  with the "Hub" section (Projects) makes `defaults.test.ts`'s `initAppNav` assertion expect that section rather than `[]`
  (HB2 — the third fill in this file). No new row (same file); the guard still catches a stray section.
- **2026-07-15** — f-project-admin t-1 (review): added upstream-asks row [sunrise#435](https://github.com/human-centric-engineering/sunrise/issues/435)
  (path-param validator proposal — a generic `validatePathParam` in core `lib/api/validation.ts`, DRYing the ~18 per-route
  `parse<X>Id` helpers). Surfaced when the first fork admin-CRUD reproduced the helper 3× in one feature. **No fork platform
  edit** — the fork DRYs its own 3 routes via a new fork-owned `lib/api/route-params.ts` (`parseCuidParam`); delete that file
  - adopt core on landing.
- **2026-07-15** — f-shell close-out: added upstream-asks rows [sunrise#433](https://github.com/human-centric-engineering/sunrise/issues/433)
  (error-boundary session-expiry double-fire — bug) + [#434](https://github.com/human-centric-engineering/sunrise/issues/434)
  (shared `<RouteErrorBoundary>` seam proposal). Both from t-1's review of the new `(hub)/error.tsx`;
  no fork platform edit (the fork corrected its own boundary; platform ones deferred to the upstream fix).
- **2026-07-14** — f-shell t-1 (shell skeleton): row 3 updated (the `(public)/page.tsx` shim
  **deleted** — `/` reclaimed as the Hub home in the new `(hub)` group) + rows 12–13 (post-login
  landing repointed to `/`: `login-form.tsx` default + `proxy.ts` authed-on-auth redirect;
  signup/OAuth/invite/verify deliberately left on `/dashboard` for its verification-status UX).
- **2026-07-11** — f-fork t-1 (auth-only shell): rows 3–7 (the marketing strip — landing
  redirect, About deletion, sitemap trim, and the two nav/footer default-test adaptations
  from curating `lib/app/public-nav.ts`).
- **2026-07-14** — Brand-seam title fix: row 11 — the `settings.ts`/`knowledge.ts` `*_TAB_TITLES` **and** the three `(public)` legal pages' metadata/body now use `${BRAND.name}` instead of the hardcoded `Sunrise` (a real platform bug found validating f-theme in the browser — the whole surface fixed in one go). Filed upstream as sunrise#432; delete row 11 on landing.
- **2026-07-14** — f-theme t-1 (base theme): row 10 — the `brand-mark.test.tsx` default-render
  adaptation, from filling the `components/brand/brand-mark.tsx` scaffold with the HCE "H" mark.
  (The theme itself lands in the fork-owned `app/brand-theme.css` seam — not a divergence.)
- **2026-07-11** — Ledger created (PR #5, the planning + f-fork-claim docs PR). Seeded with
  rows 1–2: the `defaults.test.ts` eslint assertion and the `.prettierignore` append.
