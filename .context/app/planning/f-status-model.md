---
name: f-status-model
feature: 20 / f-status-model
status: in flight        # not started | in flight | blocked | shipped
owner: Simon
opened: 2026-07-23
plan: .context/app/planning/plan.md
spec: .context/app/planning/self-hosting.md
---

# f-status-model — readiness-derived feature + task status

*Feature 20 on [[plan]] — a **corrective** feature (the way [[f-refs]] §16 was an inserted correction), surfaced browser-validating the §19 cutover: the Hub's status labels don't reflect reality. Depends on [[f-selfhost-cutover]] §19 t-2 (the cutover module whose statuses this re-authors — the cutover data is the live test-bed). **Sequenced before the §19 close-out** (owner) — no point freezing a system-of-record that mislabels its own statuses. Corrects the status handling in [[f-plan-view]], [[f-board-view]], [[f-feature-planning]].*

## Intent

Make status **reflect reality**, derived — not a hand-typed label — at both levels:

- **Task statuses collapse to `claimed | active | merged`** (owner, 2026-07-23). A task is *born* **claimed** (owned by the feature's owner) the moment you plan a claimed feature — there is no `backlog`/`available` limbo, because a task doesn't exist until a feature is claimed *and* planned. **active** = being worked; **merged** = done. "blocked" stays a *derived* overlay (a claimed task with an unmerged dependency). We can get more granular (in-PR, in-review) when [[plan#14 · `f-github-sync`|f-github-sync]] lands; three is enough now.
- **Task-level pulling goes away.** "Claim features, not tasks": you pull a **feature** ([[f-feature-planning]]'s `claim_feature`, shipped), and its tasks are yours. So the Board's *Available* column, the task-sheet **Claim** button, and the `claim_task` capability/route are retired; the sheet's action becomes **Start** (claimed → active) then **Complete** (active → merged). The assignee-vs-claimer split collapses — a task's owner is the feature's owner.
- **Feature status is derived, not labelled `planning`.** An *unclaimed* (not-started) feature is **available** (all its feature dependencies are shipped) or **blocked · waiting on `<dep>`** (a dependency isn't shipped) — the same readiness derivation the task model uses. Once claimed → `in_flight`; done → `shipped`. The static "planning" pill disappears.

*(Out of scope — parked, owner 2026-07-23: **external / Sunrise-gap dependencies.** A feature blocked by a missing Sunrise primitive would flag "blocked · Sunrise `<cap>` gap" and follow fix-on-fork → raise-issue. But **nothing in v1 is blocked by a Sunrise gap** — the plan's headline finding is "zero core→fork seams; the Hub builds entirely on existing Sunrise seams", and scheduling/agents/webhooks/workflows all exist. No consumer → don't build it (ship-nothing-unused). Design captured in [[self-hosting]]; build on the first real gap.)*

## Reconciliation with current repo reality   (required — done first)

Verified against `main` + the open §19 t-2 branch, 2026-07-23 (recon over `prisma/schema/app.prisma`, `lib/projects/{task-status,board,plan,plan-order,task-detail,feature-detail,claim-task-service}.ts`, `lib/projects/capabilities/{plan-feature,create-task,add-backlog,claim-task,next-task}.ts`, `components/hub/projects/{plan,board,task-sheet,feature-view,log}/**`, `lib/projects/{transfer,cutover}/**`, and the test tree). Each finding is a decision.

- **`TaskStatus` is a stored Postgres enum `{ backlog available claimed in_pr merged }` (`@default(backlog)`); `computeEffectiveStatus` (`task-status.ts`) is the hub every read surface routes through, layering a derived `blocked`.** → **Decision:** new stored enum **`{ claimed active merged }` (`@default(claimed)`)**. `computeEffectiveStatus` rewrites to: `merged`→merged; `active`→active; a live-claimant `claimed` with any unmerged dep → **`blocked`**, else `claimed`; a null-claimant `claimed` (erased user, SET NULL) → treated as owner-less but still `claimed`. `EffectiveStatus = 'claimed'|'active'|'merged'|'blocked'`. **Landmine — a Postgres enum change with three values removed + one added:** hand-authored `--create-only` migration (drop the column default → `ALTER TYPE`/new type + `USING` data map `backlog|available→claimed`, `in_pr→active` → set default `claimed` → drop old type), **not** a plain `prisma migrate` diff; B13 strip watch; `db:drift-check` green.
- **The stored-enum change is *unconsumed-until-complete* — it must land as one vertical.** Every writer (`plan-feature`/`create-task`/`add-backlog`), reader (`board`/`plan`/`next-task`/`task-detail`/`feature-detail`), the Board columns, the pills/types, and the cutover data reference the old values; changing the enum without them breaks the app. → **Decision:** t-1 is **one PR** (HB3 — a stored-enum migration + its whole vertical is one PR even when large), split from the feature-readiness derivation (t-2, no schema change).
- **`plan_feature` creates tasks `status: 'available'`; `create_task` → `'available'`; `add_backlog` → `'backlog'`.** → **Decision:** `plan_feature` creates tasks **`claimed`** (owned by the feature owner — the born-claimed cascade); `create_task` → `claimed`; **retire `add_backlog`** (the `backlog` value is gone; a not-yet-planned feature holds *indicative* tasks, not backlog `Task` rows — self-hosting §2). Its seed/capability/registration/tests go with it.
- **Task-level claiming is a whole surface: `claim_task` capability (`005-claim-task` seed) + `claim-task-service.ts` (`claimTask` writes `claimed` + a `TaskClaim` + journals `task_claimed`) + `POST …/tasks/[taskId]/claim` + the sheet Claim button (`canClaim = status==='available'`).** → **Decision:** **retire `claim_task`** (capability/seed/registration/barrel export) and **repurpose the service + route as `startTask` / `completeTask`** (`claimed → active`, `active → merged`) driving a contextual sheet action (**Start** when claimed, **Complete** when active). Reuse the **`task_claimed`** `ProjectEventKind` for Start (semantically "actively taken") + **`task_merged`** for Complete (both already in the enum — **no `ProjectEventKind` migration**). The `TaskClaim` row is **repurposed as the "active work" record** written on Start, so soft-collision + history survive the loss of task-claiming.
- **Board columns are hardcoded in 4 places** (`board.ts` `BOARD_COLUMNS` const + `columnTotals` literal; `board/types.ts` `BoardColumn`; `board/presentation.ts` `COLUMN_META` + `groupByColumn` + `BOARD_GRID` `repeat(5)`); routing maps `effective==='blocked' → 'backlog'`. → **Decision:** columns become **`claimed | active | merged`** (grid `repeat(3)`); a **blocked** task shows in its `claimed` column with the blocked treatment (no separate column — it's a claimed task that can't start yet); collision derives from **overlap among `active` tasks' `filesScope`** (+ the repurposed `TaskClaim`). Move all four spots together (landmine — desync risk).
- **Presentation: `TASK_TONE` maps all 6 effective values; `FEATURE_TONE`/`plan-summary` map `planning`; `StatusPill` tone is a `--signal-*` token.** → **Decision:** `TASK_TONE` → `{claimed, active, merged, blocked}`; add a **`--signal-active`** token (+ `-bg`) to the fork-owned `app/brand-theme.css` (else the pill renders an undefined var); `FEATURE_TONE`/`plan-summary` lose `planning`, gain derived `available`/`blocked` (t-2). Client type mirrors (`plan/types.ts` `TaskEffectiveStatus` + `FeatureStatus`, `board/types.ts`, `task-sheet/types.ts`, `feature-view/types.ts`) track the new sets.
- **Feature status is returned raw (`plan.ts:158-166` `PlanFeatureView.status: FeatureStatus`); the dep select fetches only `dependsOnFeatureId` (no status).** → **Decision (t-2):** derive an **effective feature status** — for a not-`in_flight`/`shipped` feature, `available` if **every** dependency feature is `shipped`, else `blocked` carrying the **unshipped dep slug(s)** for a "waiting on `<dep>`" reason. Add each dependency's `status` (+ slug) to the select. Mirror in `feature-detail.ts`. Keep `plan-order.ts` `STATUS_BAND` on the stored value (ordering unchanged). Stored `FeatureStatus.blocked` is **reserved** (the parked external model), unused by the derivation.
- **The transfer snapshot + cutover data author the old values.** `transfer/schema.ts` `taskStatus = z.enum([backlog…merged])` (`schemaVersion: 1`); `cutover/plan-data.ts` authors `merged`/`claimed`/`backlog` + `snapshot.ts` derives `claimedByUserId` off `claimed|in_pr|merged`. → **Decision:** update the `taskStatus` enum to `{claimed,active,merged}` and **keep `schemaVersion: 1`** (the transfer mechanism shipped days ago; **no persisted prod snapshot exists**, so no v1→v2 migration is owed — a pre-prod value-set correction). **Re-author the cutover statuses under the new model:** every shipped task stays `merged`; §19 `t-34` (cutover load — being built now) → **`active`**; `t-35` (slugs) → **`claimed`** (owned, not started); drop the `006`-demo `blocked` (f-morning-brief) + `help-wanted` (f-github-sync) so the derivation reads them as `available`. `snapshot.ts`'s claimant logic reworks against `{claimed,active,merged}`.
- **`log/presentation.ts` `describeEvent` branches `task_created` on `meta.status === 'backlog'`** ("added a backlog item"). → **Decision:** update to the new vocabulary (a created task is "planned", owned).

**Tier / seam hypotheses (to confirm at build):** pure leaf-app, **zero platform edits, zero upstream asks anticipated.** New/changed fork-owned: the `TaskStatus` enum + one **hand-authored data-migrating migration**, `computeEffectiveStatus` + every read surface, the write capabilities (`plan-feature`/`create-task`; retire `add_backlog`/`claim_task`), the `startTask`/`completeTask` service+route+sheet action, the Board column set + collision source, the pills/tokens/types, the effective-feature-status derivation, and the cutover/transfer re-author. **One migration** (t-1) → `/pre-pr` drift **RUN** (B13 watch). **No HB2** anticipated (the `TaskStatus` enum + capabilities are fork-owned; confirm no Sunrise default test asserts them). Reused live: the `access.ts` funnel, `claimFeature` (feature-level pull, unchanged), `recordProjectEvent`, the satellite-FK recipe.

## Promoted tasks

**Sizing — recommended 2 PRs.** t-1 is the **atomic stored-enum change + its whole vertical** (schema migration · `computeEffectiveStatus` · writers · retire task-claim + add Start/Complete · Board columns · pills/types · cutover/transfer re-author) — large but unconsumed-until-complete, so one PR (HB3). t-2 is the **feature-readiness derivation** — no schema change, a distinct review surface (derivation + feature-side display), separable and lower-risk. They're independent (either order); recommending t-1 first (it's the migration).

| ID  | Task | Files | Deps | Done-when | Status | PR |
|-----|------|-------|------|-----------|--------|----|
| t-1 | **Task status model → `claimed \| active \| merged`.** Enum + hand-authored data-migrating migration (`backlog\|available→claimed`, `in_pr→active`; default `claimed`) + drift probe check. Rewrite `computeEffectiveStatus` (+ every reader: `plan`/`board`/`next-task`/`task-detail`/`feature-detail`). Writers: `plan_feature`/`create_task` → born-`claimed`; **retire `add_backlog` + `claim_task`** (caps/seeds/registration/barrel); **`startTask`/`completeTask`** service+route+contextual sheet action (reuse `task_claimed`/`task_merged`). Board columns `claimed\|active\|merged` (+ blocked treatment, collision over `active` filesScope/`TaskClaim`). `TASK_TONE` + `--signal-active` token + type mirrors. **Re-author cutover + transfer** to the new values. | `prisma/schema/app.prisma`, `prisma/migrations/**`, `lib/projects/{task-status,board,plan,task-detail,feature-detail}.ts`, `lib/projects/capabilities/{plan-feature,create-task,next-task}.ts` (retire `add-backlog`,`claim-task`), `lib/projects/{claim-task-service→task-actions}.ts`, `app/api/v1/projects/[id]/tasks/[taskId]/**`, `lib/app/capabilities.ts`, `prisma/seeds/app/**`, `components/hub/projects/{board,task-sheet,plan,log}/**`, `app/brand-theme.css`, `lib/projects/{transfer/schema,cutover/plan-data,cutover/snapshot}.ts`, `tests/**` | §19 t-2 (#77) | new DB has `{claimed,active,merged}`; migration maps old rows + drift green; a planned feature's tasks are born `claimed`; Start flips `claimed→active`, Complete `active→merged`, each journalled; a claimed task with an unmerged dep reads **blocked**; the Board shows Claimed/Active/Merged with soft collision; `claim_task`/`add_backlog` gone (parity green); the cutover re-imports (§19 tasks read merged/active/claimed); no platform edit; gates green | in flight | — |
| t-2 | **Derived feature readiness (available / blocked · waiting-on).** `getProjectPlan` (+ `getFeatureDetail`) select each dependency's `status`+`slug` and derive an effective feature status: not-started + all deps shipped → `available`; else `blocked` with the unshipped dep(s). Feature-row/pill/`plan-summary`/`feature-view` render `available`/`blocked · waiting on <dep>` (the "planning" label goes); `FEATURE_TONE` gains `available`. | `lib/projects/{plan,feature-detail}.ts`, `components/hub/projects/plan/{feature-row,plan-summary,presentation,types}.tsx`, `components/hub/projects/feature-view/**`, `tests/**` | t-1 (co-req; either order) | an unclaimed feature whose deps are all shipped reads **available**; one with an unshipped dep reads **blocked · waiting on `<dep>`**; a claimed feature stays `in_flight`, shipped stays `shipped`; the "planning" pill is gone; §16–§19 read correctly (f-intake blocked-on-f-sidekick; f-sidekick/f-github-sync/f-morning-brief available); **owner browser-validates**; gates green | backlog | — |

*Standing steps in each Done-when:* vitest strategy below; `commit → /pre-pr → /security-review → push → open PR → /code-review`; `gh pr create --repo human-centric-engineering/hce-hub`. **One migration** (t-1) → `/pre-pr` drift **RUN** (B13 watch). **No** HB2 / `platform-divergences` row anticipated.

## Test strategy

vitest = happy-dom, no live DB ([[planning-retro]] B9): mock `@/lib/db/client` / `tx`; the enum migration is proven by `db:drift-check` + the erasure smoke, and end-to-end by re-running `app:project:import-plan` (the cutover as the live test-bed); components via `@testing-library/react`.

- **`computeEffectiveStatus` (t-1, load-bearing):** every stored value (`claimed`/`active`/`merged`), the derived `blocked` (claimed + unmerged dep), the null-claimant case, no-deps; the readers (`board` routing, `plan` progress, `next-task`, task/feature-detail) reflect it.
- **Writers + actions (t-1):** `plan_feature` births `claimed` tasks; `create_task` → `claimed`; `startTask` `claimed→active` + `completeTask` `active→merged`, each emitting the right event; a non-member → `not_found`; class↔seed **parity** green after `claim_task`/`add_backlog` removal.
- **Board (t-1):** three columns; a blocked task shows in Claimed with the blocked treatment; collision over active `filesScope`; totals match.
- **Migration/data (t-1):** the cutover re-authors + `app:project:import-plan` re-imports cleanly (spot-check §19 t-34 `active`, t-35 `claimed`); the transfer round-trip validates the new enum.
- **Feature readiness (t-2):** all-deps-shipped → `available`; an unshipped dep → `blocked` naming it; claimed→`in_flight`; the §16–§19 fixture reads correctly; a null owner never derefs.

## Open questions

- **Resolved inline:** the enum + migration mapping (recon #1); one-vertical sizing (recon #2); born-claimed + retire `add_backlog` (recon #3); retire `claim_task` → Start/Complete, reuse existing event kinds, repurpose `TaskClaim` (recon #4); the 3-column Board (recon #5); tones/tokens/types (recon #6); derived feature readiness (recon #7); transfer `schemaVersion` stays v1 + cutover re-author (recon #8).
- **For the owner to confirm (at claim review):**
  1. **Sizing: 2 PRs** — t-1 (task-status model, one atomic vertical) · t-2 (feature readiness). Confirm, or split t-1's Board/actions from the schema (not recommended — the enum change breaks them if separated).
  2. **A manual `Complete` (active → merged) action now** — so the flow is fully drivable in the Hub (claimed → active → merged all reachable by hand), with `f-github-sync` later automating Complete on PR-merge. **Recommended.** (Alternative: only `Start`, leaving tasks stuck at `active` until `f-github-sync` — not recommended; it breaks the live dogfood.)
  3. **Soft-collision after task-claiming is removed.** **Recommended:** `Start` writes a repurposed `TaskClaim` ("active work" record) so the existing `filesOverlap` collision + history survive; collision reads over `active` tasks. (Alternative: derive collision purely from `active` tasks' `filesScope`, drop `TaskClaim` — simpler but loses the claim-history row.)

## Upstream follow-ups / seam ledger

**None anticipated.** Pure leaf-app: the `TaskStatus` enum + a fork-owned migration, the fork's own capabilities/services/routes/UI, and the cutover/transfer re-author — all through the existing `access.ts` funnel + `recordProjectEvent`. No core→fork seam, no platform-file edit, no upstream issue. *(Watch: the enum migration is the risk — hand-author the `ALTER TYPE` + `USING` data map `--create-only`, strip B13 spurious drops, `db:drift-check` green. The **external/Sunrise-gap** model is parked, not owed.)*

## Decisions log   (append-only, newest first)

- **2026-07-23 — Claimed + planned (owner Simon).** A corrective feature surfaced browser-validating the §19 cutover — the Hub's statuses don't reflect reality. **Owner decisions that shaped it (2026-07-23):** (1) task statuses collapse to **`claimed | active | merged`**, tasks born `claimed` at plan time (the feature-claim cascade), `blocked` stays a derived overlay; (2) **task-level pulling is removed** ("claim features, not tasks") — the Board *Available* column, the sheet **Claim**, and `claim_task` retire; the action becomes **Start** then **Complete**; (3) **feature status is derived** — unclaimed features read `available`/`blocked · waiting on <dep>`, the "planning" label goes; (4) the **external/Sunrise-gap** dependency model is **parked** (no v1 consumer — the Hub builds on existing Sunrise seams; scheduling/agents/webhooks/workflows all exist). Reconciled against `main` + the §19 t-2 branch: the `TaskStatus` enum change is a **hand-authored data-migrating Postgres migration** (the single risk) and an **unconsumed-until-complete vertical** → t-1 one PR (schema+logic+actions+Board+cutover), t-2 the feature-readiness derivation. Reuses existing `task_claimed`/`task_merged` event kinds (no `ProjectEventKind` migration) + `claimFeature` (unchanged). Transfer `schemaVersion` stays v1 (pre-prod, no persisted snapshot). **HB8 owner decisions surfaced:** sizing (2 PRs), a manual `Complete` action now, soft-collision via a repurposed `TaskClaim`. **Sequenced before the §19 close-out** (don't freeze a mislabelled record); §19 t-3 (slugs) follows. Depends on §19 t-2 (#77) merging (it re-authors the cutover statuses). **Recommended 2 PRs** — pending owner confirmation. **No platform edit / no HB2 / no upstream ask** anticipated (pure leaf-app).
