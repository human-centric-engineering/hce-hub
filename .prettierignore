# Dependencies
node_modules

# Build outputs
.next
out
build
dist

# Caches
.cache
.turbo

# Package manager files
package-lock.json
yarn.lock
pnpm-lock.yaml

# Environment
.env*
!.env.example

# Generated files
*.generated.*
.vercel
.swc

# Prisma migrations (generated SQL)
prisma/migrations/**/*.sql

# Public assets
public

# Documentation
CHANGELOG.md

# Coverage
coverage
.nyc_output

# Agent tooling artefacts — specific format preserved for AI agents
# The remediation ledger uses column-aligned pipe tables that Prettier collapses,
# which breaks the deterministic read-parse-write round-trip used by /test-triage.
.claude/testing/remediation-ledger.md

# Per-developer local Claude Code settings (gitignored, written by the CLI).
# Format is owned by the tool, so Prettier should not gate `npm run validate` on it.
.claude/settings.local.json

# HCE Hub (app) planning tree — fork-owned docs + design-handoff reference.
# The design handoff bundles prototype .jsx/.css/.html (their own standalone
# style, not production code) and the planning markdown uses column-aligned
# Obsidian tables/wikilinks that Prettier collapses. It's authoring material with
# no build impact, so Prettier should not gate `npm run validate` on it.
.context/app/planning/
